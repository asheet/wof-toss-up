# Copy this to .env and update with your server details

# AI Game Host Configuration for Llama 3.2-3b
AI_API_KEY=51700ba8612f8f9962182ef6c9ba63fe
AI_BASE_URL=https://llama-3-2-3b-maas-apicast-production.apps.prod.rhoai.rh-aiservices-bu.com:443/v1
AI_MODEL=llama-3-2-3b

# Set to true to enable AI host
AI_HOST_ENABLED=true

# Fallback to static messages if AI fails
FALLBACK_TO_STATIC=true

# Text-to-Speech Configuration using Higgs Audio v2 via vLLM
# Enable TTS for AI game host audio responses
TTS_ENABLED=true

# Higgs Audio v2 model - state-of-the-art TTS with 75.7% win rate over GPT-4o-mini
TTS_MODEL=higgs-audio-v2-generation-3B-base

# vLLM server endpoint for Higgs Audio v2 (use your external server URL)
TTS_BASE_URL=http://your_vllm_server:port

# API key for your vLLM server (if required)
TTS_API_KEY=your_vllm_api_key_here

# Voice settings (currently used for logging/identification)
TTS_VOICE=game_host
